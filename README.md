# Calculator client-server 

- Client (in console): make requests to the server.
- Server (api): get requets via POST, validate and respond the request.
- Library: entities which both client and server use.


# Things of interest

> RestSharp (for el conexion)
> Nlog (for logs)